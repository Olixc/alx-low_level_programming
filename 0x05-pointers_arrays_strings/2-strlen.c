#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: The string s value
 *
 * Return: length of the string.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
