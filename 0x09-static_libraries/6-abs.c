#include <stdio.h>
#include <stdlib.h>

#include "main.h"

/**
 * _abs - computes the absolute of an integer
 * @n: The number to print
 *
 * Return: absolute number of n;
 */

int _abs(int n)
{
	return (abs(n));
}
